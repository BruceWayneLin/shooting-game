import * as PIXI from 'pixi.js';
import cloudMap from '../../assets/cloud.png';
class CloudOne extends PIXI.TilingSprite { 
    // protected Cloud: PIXI.TilingSprite(PIXI.Texture.from(cloudMap), 490, 350);

    constructor(params: any) { 
        super();
    }
}

export default CloudOne;